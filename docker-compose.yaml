services:
  content-api:
    build:
      context: .
      args:
        - CACHE_BUST=20260501v9-persist
    container_name: content-factory-v5
    ports:
      - "8085:8000"
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - /opt/content-factory/output:/app/output
