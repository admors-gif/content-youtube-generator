version: '3.8'

services:
  content-api:
    build: .
    container_name: content-factory-api
    ports:
      - "8000:8000"
    restart: unless-stopped
    # Las variables de entorno reales serán inyectadas por el panel de Hostinger
