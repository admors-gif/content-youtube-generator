services:
  content-api:
    build:
      context: .
      args:
        - CACHE_BUST=20260501v8-envfix2
    container_name: content-factory-v5
    ports:
      - "8085:8000"
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ./output:/app/output
